---
  platform: linux
  image_resource:
    type: docker-image
    #source: {repository: node, tag: 0.10.46-wheezy}
    source: {repository: alpine}

  inputs:
    - name: resource-app

  run:
    path: resource-app/command.sh
